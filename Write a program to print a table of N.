#include<stdio.h>
int main()
{
    int num;
    printf("Enter which table you want to print ");
    scanf("%d",&num);
    for(int i=1;i<=10;i++)
        printf("%d*%d is %d\n",num,i,num*i);
}
